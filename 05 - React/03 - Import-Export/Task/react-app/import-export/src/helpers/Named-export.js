export function heloo(name, surname) {
    return (`Heloo, my name is ${name} ${surname}`);
};

export function divide(a, b) {
    return (a / b);
}

export const dogName = "Ami";
export const dogAge = 2;